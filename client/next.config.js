/** @type {import('next').NextConfig} */
const nextConfig = {
  // Don't require NEXT_PUBLIC_API_URL at build time
  // We'll detect the backend URL at runtime instead
}

module.exports = nextConfig







