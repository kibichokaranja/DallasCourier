[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]
# Note: NEXT_PUBLIC_API_URL is not required - we use runtime detection

[start]
cmd = "npm start"

